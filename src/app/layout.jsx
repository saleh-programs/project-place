import "styles/index.css";


export const metadata = {
  title: "Project Place",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}
